# PDF Generator App

Приложение для генерации PDF-документов из CSV/JSON данных с использованием HTML-шаблонов и web-интерфейсом на базе Streamlit.

## 🚀 Возможности

- **Парсинг данных**: Поддержка CSV и JSON файлов с автоматическим определением кодировки и разделителей
- **Генерация PDF**: Конвертация HTML в PDF с поддержкой кириллицы и различных шрифтов
- **Web-интерфейс**: Современный интерфейс на Streamlit с вкладками для удобной работы
- **Шаблонизация**: Использование Jinja2 для динамического наполнения шаблонов
- **История генераций**: SQLite база данных для хранения истории с возможностью фильтрации
- **Пакетная генерация**: Возможность генерации нескольких PDF одновременно с архивацией
- **Редактирование шаблонов**: Встроенный редактор HTML-шаблонов через web-интерфейс

## 📋 Требования к системе

- Python 3.9+
- Операционная система: Windows 10/11, macOS Ventura+, Linux

## 🛠️ Установка

1. **Клонируйте репозиторий** или скачайте файлы проекта

2. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте тестовые данные** (опционально):
   ```bash
   python create_test_data.py
   ```

4. **Запустите приложение**:
   ```bash
   streamlit run app.py
   ```

5. **Откройте браузер** по адресу `http://localhost:8501`

## 📖 Использование

### Основной рабочий процесс

1. **Выбор файла данных**:
   - Выберите существующий CSV/JSON файл из директории `/data`
   - Или загрузите новый файл через drag-and-drop
   - Просмотрите превью данных

2. **Выбор шаблона**:
   - Выберите HTML-шаблон из директории `/templates`
   - Просмотрите превью шаблона
   - При необходимости отредактируйте шаблон

3. **Генерация PDF**:
   - Выберите ID счета для генерации
   - Нажмите "Сгенерировать PDF"
   - Скачайте или откройте готовый PDF

### Структура данных

#### Обязательные поля
- `invoice_id` — уникальный идентификатор счета
- `customer_name` — имя покупателя
- `date` — дата в формате ДД.ММ.ГГГГ
- `items` — список товаров (массив объектов)

#### Структура товара
- `product_name` — наименование товара
- `quantity` — количество
- `price` — цена за единицу
- `total` — общая стоимость (опционально, рассчитывается автоматически)

#### Опциональные поля
- `company_name` — название компании
- `address` — адрес
- `phone` — телефон
- `email` — email
- `grand_total` — итоговая сумма

### Шаблоны

В HTML-шаблонах используются placeholders Jinja2:

```html
{{ invoice_id }}       <!-- ID счета -->
{{ customer_name }}    <!-- Имя покупателя -->
{{ date }}             <!-- Дата -->
{{ company_name }}     <!-- Компания -->
{{ address }}          <!-- Адрес -->
{{ grand_total }}      <!-- Итого -->

<!-- Цикл по товарам -->
{% for item in items %}
  {{ item.product_name }}
  {{ item.quantity }}
  {{ item.price }}
  {{ item.total }}
{% endfor %}
```

## 📁 Структура проекта

```
project/
├── app.py                  # Основной файл с web-интерфейсом (Streamlit)
├── pdf_generator.py        # Модуль генерации PDF
├── data_parser.py          # Модуль парсинга CSV/JSON
├── database.py             # Модуль работы с БД (SQLite)
├── create_test_data.py     # Скрипт создания тестовых данных
├── requirements.txt        # Зависимости проекта
├── history.db              # База данных истории (создается автоматически)
├── README.md               # Документация
├── /data                   # Директория с файлами данных
│   ├── invoices_sample1.csv
│   ├── invoices_sample2.csv
│   ├── orders_sample1.json
│   └── orders_sample2.json
├── /templates              # Директория с HTML-шаблонами
│   ├── invoice_template.html
│   ├── order_template.html
│   └── report_template.html
└── /output                 # Директория для готовых PDF
```

## 🎯 Примеры использования

### Генерация счета из CSV

1. Выберите файл `invoices_sample1.csv`
2. Выберите шаблон `invoice_template.html`
3. Выберите ID счета `INV-2025-001`
4. Нажмите "Сгенерировать PDF"

### Генерация заказа из JSON

1. Выберите файл `orders_sample1.json`
2. Выберите шаблон `order_template.html`
3. Выберите ID счета `ORD-2025-101`
4. Нажмите "Сгенерировать PDF"

### Пакетная генерация

1. Выберите файл данных и шаблон
2. Перейдите на вкладку "Генерация PDF"
3. Выберите несколько ID счетов
4. Нажмите "Сгенерировать все выбранные PDF"
5. Скачайте ZIP-архив со всеми PDF

## 🔧 Настройки

### Формат страницы
- A4 (по умолчанию)
- Letter

### Ориентация
- Portrait (по умолчанию)
- Landscape

### Ограничения
- Максимальный размер загружаемого файла: 50MB
- Поддерживаемые форматы файлов: .csv, .json, .html

## 📊 История генераций

Приложение сохраняет историю всех генераций в SQLite базу данных:

- Дата и время генерации
- ID счета и имя покупателя
- Использованный файл данных и шаблон
- Путь к выходному файлу
- Статус генерации (успех/ошибка)

### Функции истории
- Фильтрация по дате, ID счета, шаблону
- Скачивание PDF повторно
- Открытие PDF в системной программе
- Удаление записей
- Статистика генераций

## 🛡️ Безопасность

- Валидация загружаемых файлов по типу и размеру
- Санитизация имен файлов
- Защита от SQL-инъекций через параметризованные запросы
- Проверка HTML-шаблонов на потенциально опасный код

## ❓ Часто задаваемые вопросы

**Q: Как добавить новый шаблон?**
A: Загрузите HTML файл через интерфейс или поместите в директорию `/templates`.

**Q: Поддерживаются ли другие форматы данных?**
A: В текущей версии поддерживаются только CSV и JSON. Для других форматов требуется доработка.

**Q: Можно ли использовать собственные шрифты?**
A: Да, укажите их в CSS шаблонов через `@font-face` с URL или локальными путями.

**Q: Как экспортировать историю?**
A: Функция экспорта будет добавлена в следующих версиях. Пока можно работать с БД напрямую.

**Q: Возможна ли интеграция с внешними сервисами?**
A: Да, через расширение модулей можно добавить API интеграции.

## 📝 Лицензия

MIT License

## 👨‍💻 Автор

Создано с использованием Python, Streamlit, WeasyPrint, Jinja2 и других современных технологий.

---

**Версия:** 1.0.0
**Дата:** 2025
